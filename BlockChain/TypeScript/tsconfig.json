{
    // js -> ts로 변환하는 역할
    "compilerOptions": {
      "module": "commonjs",
      "target": "ES2015",
      "sourceMap": true,
    // 모든 파일이 아래 경로로 추출됨
      "outDir": "dist"
    },
    // 경로추가
    // src 내부에 있는 모든 파일들을 읽어온다.
    // "include": ["src/index.ts"],
    "include": ["src/**/*"],
    "exclude": ["node_modules"]
  }